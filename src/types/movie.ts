export type Movie = {
    id: number; // ID is a number based on the API response
    movie: string;
    rating: number;
    image: string; // Image is a relative path or URL
    imdb_url: string;
};
